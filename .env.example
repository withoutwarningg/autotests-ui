# Скопируйте в .env и при необходимости поменяйте — .env в git не попадает.
# Ничего из этого не обязательно: по умолчанию тесты идут на saucedemo.com.

# UI_BASE_URL=https://www.saucedemo.com
# UI_TIMEOUT_MS=10000
